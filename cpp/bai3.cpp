#include <bits/stdc++.h>

using namespace std;

signed main() {
    cin.tie(NULL)->sync_with_stdio(false);

    cin >> N;
    for (i = 1; i < N; i++) {
        cin >> x >> y >> t >> c;
        q.push(t, x, y
    }
}
